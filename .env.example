# Environment Configuration Template for Zabbix Monitor
# Copy this file to .env and customize for your environment
# DO NOT commit .env file to version control

# === REQUIRED CONFIGURATION ===

# Your Zabbix monitoring server domain or IP
ZABBIX_SERVER_DOMAIN="your-monitor-server.example.com"

# SSH tunnel configuration
SSH_TUNNEL_PORT="2022"
SSH_TUNNEL_USER="zabbix-user"

# Zabbix server port (usually 10051)
ZABBIX_SERVER_PORT="10051"

# === OPTIONAL CONFIGURATION ===

# Zabbix version to install (default: 6.4)
ZABBIX_VERSION="6.4"

# Custom banner text for server identification
CUSTOM_BANNER_TEXT="Managed Server - Contact IT Support"

# === SECURITY NOTES ===

# 1. Use non-default SSH ports (avoid 22, 2222, 20202)
# 2. Create unique SSH usernames (avoid 'zabbix', 'zabbixssh', 'monitoring')
# 3. Ensure your monitoring server domain has proper DNS configuration
# 4. Use strong SSH keys (RSA 4096-bit or ED25519)

# === EXAMPLE PRODUCTION CONFIGURATION ===

# ZABBIX_SERVER_DOMAIN="monitor-prod.yourcompany.com"
# SSH_TUNNEL_PORT="7832"
# SSH_TUNNEL_USER="monitoring-agent-prod"
# ZABBIX_SERVER_PORT="10051"
